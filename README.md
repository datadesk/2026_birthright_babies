# birthright_babies

Small project for processing IPUMS data for birthright babies analysis.

Project layout
- `script/` — Python scripts and workspace files.
- `data/` — raw and processed data (keep out of repo if large).
- `output/` — generated results and exports.

